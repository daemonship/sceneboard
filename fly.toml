# Fly.io configuration for SceneBoard
app = "sceneboard"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8000"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[http_service.checks]]
  interval = "1m"
  timeout = "5s"
  grace_period = "5s"
  method = "GET"
  path = "/"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256

# iCal feed ingestion — run every 6 hours with a scheduled Fly Machine:
#
#   fly machine run --app sceneboard \
#     --schedule every_6h \
#     --entrypoint "python manage.py poll_ical_feeds" \
#     .
#
# See: https://fly.io/docs/machines/run-a-machine-on-a-schedule/
